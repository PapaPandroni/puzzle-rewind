DATABASE_URL=sqlite+aiosqlite:///./dev.db
# Optional: personal Lichess API token (no scopes needed), sent as a Bearer
# header on outbound requests to raise our shared rate-limit ceiling with
# Lichess. Create one at https://lichess.org/account/oauth/token/create
LICHESS_TOKEN=
